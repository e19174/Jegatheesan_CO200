import React from 'react';
import TransferForm from './TransferForm'; // Assuming TransferForm.js is in the same directory

const App = () => {
    return (
        <div className="App">
            <TransferForm />
        </div>
    );
};

export default App;

